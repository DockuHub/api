class UserController {
  constructor() {
    // We can pass the manager if we want some sort of state, if we don't it's all the same
  }

  public search() {
    // this.userManager.search(); | UserManager.search();
  }

  public save() {
    // this.userManager.save() | UserManager.save();
  }
}
