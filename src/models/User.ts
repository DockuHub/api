abstract class DopeAssModel {}

class User extends DopeAssModel {
  public documents() {
    // Return the relationship to documents using methods from DopeAssModel such as one to one, has many etc
  }
}
